Load AI-optimized workflow context for beads issue tracking.

Outputs essential beads workflow rules and command reference to help agents remember to use br instead of markdown TODOs after context compaction.

```bash
br prime
```
